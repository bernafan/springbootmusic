spring:
   datasource:
      url:  jdbc:mysql://localhost:3306/dbspotmusic?useSSL=false&createDatabaseIfNotExist=true&useTimezone=true&serverTimezone=UTC
      username: root
      password: rootroot
      driver-class-name: com.mysql.jdbc.Driver
   jpa:
      properties:
         hibernate:
            show_sql: true
            hbm2ddl:
               auto: update